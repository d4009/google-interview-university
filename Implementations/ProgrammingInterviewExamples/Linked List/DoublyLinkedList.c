#include <stdio.h>

struct ListData {
    int n;
};

struct DoublyLinkedList {
    struct ListData data;
    struct DoublyLinkedList* next;
    struct DoublyLinkedList* prev;
}DoublyLinkedList;

void main(void){

}
